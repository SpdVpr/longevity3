// This file is used by next-intl in the App Router
// Define locales directly - English only
const locales = ['en'];
const defaultLocale = 'en';

module.exports = {
  locales,
  defaultLocale
};
